VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=YOUR_ANON_KEY
VITE_WHOOP_CLIENT_ID=
VITE_WHOOP_REDIRECT_URI=http://localhost:5173/oauth/callback

# Edge function / server secrets (set via: supabase functions secrets set KEY=VALUE)
# SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
# SUPABASE_ANON_KEY=YOUR_ANON_KEY
# WHOOP_CLIENT_ID=
# WHOOP_CLIENT_SECRET=
# WHOOP_REDIRECT_URI=http://localhost:5173/oauth/callback
# OPENAI_API_KEY=
# --- Frontend (Vite) Environment Variables ---
# Rename this file to `.env` (for all modes) or `.env.local` (gitignored) and fill values.
# Only variables prefixed with VITE_ are exposed to the browser.

# Supabase project URL (found in Supabase settings > API)
VITE_SUPABASE_URL=
# Supabase anon (public) key
VITE_SUPABASE_PUBLISHABLE_KEY=

# (Optional) AI provider public (non-secret) config flags go here.
# Example (do NOT expose secret keys to browser!)
# VITE_OPENAI_MODEL=gpt-4o-mini

# --- Supabase Edge Functions / Server-side Environment ---
# Set these via `supabase secrets set` or your hosting provider (DO NOT commit real values).
# They are used inside edge functions (Deno.env.get(...))
# supabase functions secrets set SUPABASE_URL=... SUPABASE_ANON_KEY=... SUPABASE_SERVICE_ROLE_KEY=...

# Base Supabase URL and keys (duplicated for edge functions runtime)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# If using an AI provider set the secret key ONLY as a server/edge function secret (e.g. OPENAI_API_KEY, ANTHROPIC_API_KEY, GEMINI_API_KEY).
# Do NOT put those secret API keys in this file unless they are prefixed with something NOT exposed (remove VITE_ and keep out of repository).
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GEMINI_API_KEY=

# --- Other Suggested Variables ---
# App environment identifier (development|staging|production)
VITE_APP_ENV=development
# Optional logging level
VITE_LOG_LEVEL=info
